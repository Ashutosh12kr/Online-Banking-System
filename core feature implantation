🔹 Core Feature Implementation

The Online Banking System is designed using Core Java concepts, where each banking feature is implemented through a structured combination of OOP, Event Handling, Multithreading, JDBC, and Data Validation.

1️⃣ Account Creation
🔧 Implementation

User enters name, password, and initial balance

A random unique account ID is generated

SavingsAccount object is created

Data is stored in MySQL database using DAO

💻 Code Logic
Account acc = new SavingsAccount(id, name, password, balance);
dao.save(acc);

✔ Concepts Used

Constructor

Inheritance

JDBC

Exception Handling

2️⃣ User Login Authentication
🔧 Implementation

User enters Account ID and Password

Account is fetched from database

Password verified using checkPassword()

Session stored in current object

💻 Code Logic
current = dao.get(id);
((Account)current).checkPassword(password);

✔ Concepts Used

Encapsulation

Type Casting

Data Validation

3️⃣ Deposit Money
🔧 Implementation

User enters amount

Amount validated

Deposit executed in separate thread

Balance updated in database

💻 Code Logic
current.deposit(amount);
dao.update(current);

✔ Concepts Used

Multithreading

Synchronization

Exception Handling

4️⃣ Withdraw Money
🔧 Implementation

Checks for sufficient balance

Withdrawal executed using thread

Prevents negative balance

💻 Code Logic
current.withdraw(amount);

✔ Concepts Used

Polymorphism

Runtime Exception Handling

Synchronization

5️⃣ Balance Enquiry
🔧 Implementation

Balance fetched directly from object

Displayed on GUI text area

💻 Code Logic
output.setText("Balance: " + current.getBalance());

✔ Concepts Used

Getter Methods

GUI Output Handling

6️⃣ Multithreaded Transaction Processing
🔧 Implementation

Each transaction runs in TransactionThread

Synchronization avoids race condition

Ensures thread safety

💻 Code Logic
synchronized(acc) {
   acc.deposit(amt);
}

✔ Concepts Used

Thread Class

Synchronization

Shared Resource Handling

7️⃣ Database Connectivity (JDBC)
🔧 Implementation

DBConnection manages database connection

AccountDAO performs CRUD operations

💻 Code Logic
Connection con = DBConnection.getConnection();
PreparedStatement ps = con.prepareStatement(...);

✔ Concepts Used

JDBC API

PreparedStatement

DAO Pattern

8️⃣ Event Handling & Processing
🔧 Implementation

Buttons generate events

ActionListener captures events

Methods process logic

💻 Code Logic
create.addActionListener(e -> createAccount());

✔ Concepts Used

Event Listener Model

Lambda Expressions

Swing Components

9️⃣ Servlet-Based Login (Web Support)
🔧 Implementation

LoginServlet handles HTTP POST request

Credentials validated

Response sent to client

💻 Code Logic
protected void doPost(HttpServletRequest req,
                      HttpServletResponse res)

✔ Concepts Used

Servlet API

HTTP Request–Response Model

🔹 Core Features Summary Table
Feature	Implementation
Account Creation	OOP + JDBC
Login Authentication	Encapsulation
Deposit	Threads + Sync
Withdraw	Exception Handling
Balance Enquiry	Getter Methods
GUI	Swing
Database	MySQL + JDBC
Web Login	Servlet
